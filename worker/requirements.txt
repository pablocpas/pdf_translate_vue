# requirements.txt

# --- Core ML Dependencies ---
# PyTorch (se instalará con un comando específico en el Dockerfile para la versión CPU)
# torch
# torchvision
# torchaudio

doclayout-yolo
huggingface-hub
opencv-python-headless  # Usamos headless para entornos de servidor sin GUI
shapely
Pillow
openai
# --- Application Dependencies (Ejemplo) ---
# Mantén las dependencias de tu aplicación, como Celery
celery
redis
pdf2image==1.17.0
numpy==1.24.4
boto3>=1.26.0
botocore>=1.29.0
pydantic>=2.0.0
pydantic-settings
pytesseract==0.3.13
reportlab==4.2.5
fvcore==0.1.5.post20221221
aiohttp